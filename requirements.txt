# Data/preprocess compatibility input only. The application runtime is pinned by
# backend/requirements.lock; keep this file intentionally small and do not use it
# as a production deployment lock.
requests
bs4
PyPDF2
python-dotenv
datasets
parlant
httpx
lxml
moto
pymongo
dnspython>=2.0.0,<3.0.0
sentence-transformers
deep-translator
toon-format==0.9.0b1
